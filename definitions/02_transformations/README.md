# 02_transformations — Silver Layer

Planned tables sourced from `ga4_events_flat`:

| File | Description |
|---|---|
| `ga4_sessions.sqlx` | One row per session |
| `ga4_transactions.sqlx` | One row per purchase |
| `ga4_items.sqlx` | One row per item (UNNEST items array) |
| `ga4_pageviews.sqlx` | One row per page_view event |
